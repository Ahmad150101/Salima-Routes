{
  "name": "salima-routes",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-js.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm test"
  }
}
